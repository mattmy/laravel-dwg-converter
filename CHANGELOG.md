# Changelog

## Unreleased

- Add DWG to DXF, SVG, and embedded-thumbnail operations backed by LibreDWG.
- Bind each source at `toDxf()`, `toSvg()`, or `thumbnail()` and keep terminal verbs argument-free.
- Add bounded temporary workspaces, one-time outputs, Laravel Storage streaming, and stable package failures.
