# Contributing

Run `composer validate --strict`, `vendor/bin/pint --test`, `vendor/bin/phpstan analyse`, `vendor/bin/pest`, and `composer audit` before submitting a change. Do not add LibreDWG binaries to the package archive.
